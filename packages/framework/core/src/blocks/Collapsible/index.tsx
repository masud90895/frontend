/**
 * @type: ui
 * name: core.block.listviewCollapsible
 * chunkName: listview
 */
import { withPagination } from '@metafox/framework';
import Base from './Base';

export default withPagination(Base);
