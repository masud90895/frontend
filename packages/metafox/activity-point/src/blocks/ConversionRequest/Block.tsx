/**
 * @type: block
 * name: activity-point.block.conversion_request
 * keyword: activity point
 * title: conversion request
 */

import { createBlock } from '@metafox/framework';
import Base, { Props } from './Base';

export default createBlock<Props>({
  extendBlock: Base
});
